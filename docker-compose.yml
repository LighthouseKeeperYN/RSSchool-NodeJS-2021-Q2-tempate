version: '3'
services:
  node:
    build: .
    ports:
      - ${PORT}:${PORT}
    container_name: rest-service
    environment:
      - PORT=${PORT}
    restart: always
    volumes:
      - ./src:/usr/app/src
      - ./logs:/usr/app/logs
      - ./db:/usr/app/db
    networks:
      - my-network
networks:
  my-network:
    driver: bridge
volumes:
  pgdata:
